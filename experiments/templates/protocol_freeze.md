# 先导协议冻结表

模板，无实测参数。复制到具体实验目录填写，模板原件留空。实际状态：草案 / 开发试采 / 已冻结待运行 / 已运行待复核。选择后填写依据，不预先标记现场就绪。

## 目标与版本

- 任务、负责人、复核人、当前阶段：
- 协议版本（建议 `pilot-v0.2`）、代码提交、冻结日期、数据版本：
- 假设、主要对照、比较层（视角机制 / 完整系统）：
- 主结果：截止确认清单的独立芽发现率；辅助指标及用途：

## 硬件、场景与采集

- 开发板/相机/平台实测配置、可用范围与证据：
- 位姿误差、回差、稳定等待、校准版本、坐标变换方向和判据依据：
- 茶树品种、访问日期、核验操作条件、复核人：
- 区域、坐标系、芽梢归属点、等级定义、边界/邻芽/未决样例：
- 允许动作集、初始位姿、每事件帧数/推理次数、失败事件计费：
- 场景版本、批次质检规则、参考复拍、变化判据：
- 全部非破坏观察结束后才改变场景的执行顺序：

## 数据与信息隔离

- 开发/校准/验证植株、区域、日期划分及版本：
- 彩色/深度、配准网格/单位/无效值、时间语义、位姿、日志位置与文件哈希索引；原始左右图/双目标定按方法需要另列：
- 算法输入、评价专用信息；阻止读取真值/未选视图的措施：
- 回放重复位姿规则、近似稳定依据、时间估计及成本模型版本：

## 方法、预算与评价

- 检测、深度、关联、存在状态更新、确认/删除规则版本；观测级原始候选门槛：
- 基线来源/代码提交、依赖检查、适配差异和调参范围：
- 主预算轴/点，辅助预算点，时间模式、共同起点、初扫计费与截止发布规则：
- 精确率下限及适用预算范围、聚合方式、空输出处理：
- 校准流程；反馈规则改变后的重新运行措施：
- 真值匹配规则、容差/身份依据、邻近匹配冲突处理：
- 真值未决项与影响范围，可观测性声明所限的视图集：
- 快照和输出/匹配文件位置，迟到/删除/重复的记录方式：
- 记录程序验收的实际日期、结果、证据（未执行时写未执行）：

## 重复与决策

- 先导植株/区域/日期数选择依据，策略顺序与随机种子：
- 配对和聚类统计单位、不确定区间方法及局限：
- 最小值得投入的收益、成本依据和继续/停止/证据不足判据：
- 加测时间点、资源上限和优先补测项：
- 尚未满足的进入条件、仍可开展的准备活动：
- 冻结后改动、版本升级和重跑规则：
